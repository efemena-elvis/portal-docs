{
  "name": "vesicash-docs-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=portal-docs",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "generate": "turbo run generate"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.6.1"
}
