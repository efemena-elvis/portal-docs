name: Deploy escrow-docs

# Only triggers when something this app actually depends on changes: its own
# folder, shared packages, workspace-level config, or this workflow itself.
# Editing another app under apps/** will NOT trigger this.

on:
  push:
    branches: [main, develop]
    paths:
      - "apps/escrow-docs/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - "package.json"
      - ".github/workflows/deploy-escrow-docs.yml"
      - ".github/workflows/deploy-app-vercel.yml"
  pull_request:
    paths:
      - "apps/escrow-docs/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - "package.json"
      - ".github/workflows/deploy-escrow-docs.yml"
      - ".github/workflows/deploy-app-vercel.yml"
  workflow_dispatch:

concurrency:
  group: deploy-escrow-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy-app-vercel.yml
    with:
      app_name: escrow-docs
      vercel_project_id: prj_nOKQOjbDIYsbhm9BDWBgw59x1Ptc
      production: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') && 'true' || 'false' }}
    secrets:
      vercel_token: ${{ secrets.VERCEL_TOKEN }}
      vercel_org_id: ${{ secrets.VERCEL_ORG_ID }}
      cf_account_id: ${{ secrets.CF_ACCOUNT_ID }}
      r2_access_key_id: ${{ secrets.R2_ACCESS_KEY_ID }}
      r2_secret_access_key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      admin_access_code: ${{ secrets.ESCROW_DOCS_ADMIN_ACCESS_CODE }}
      admin_jwt_secret: ${{ secrets.ESCROW_DOCS_ADMIN_JWT_SECRET }}
      r2_bucket_name: ${{ secrets.ESCROW_DOCS_R2_BUCKET_NAME }}
